/* Problem: You are given two strings as arguments.
 Join the strings and return the result. */

 const solve = (strOne, strTwo) => {
    return strOne + strTwo;
};

console.log(solve('hello', 'world'));
