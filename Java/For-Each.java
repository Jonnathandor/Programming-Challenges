public class ForEach {
    // The for-each loop provides a mechanism for executing a block of
    // code for each element in a collection. 

    // for(declaration: collection) {
    //     body;
    // }

    // The declaration declares the variable used to hold the values
    // assigned from the collection.

    // The collection is an array or collection holding
    // the values that will be assigned to the loop variable.

    // The body contains the statements that will be executed
    // once for each value in the collection.

    // For example:

    // char[] vowels = {'a', 'e', 'i', 'o', 'u'};

    // for(char vowel: vowels) {
    //     System.out.println(vowel);
    // }

    // A for-each loop is guaranteed to iterate over all values.
    
}
